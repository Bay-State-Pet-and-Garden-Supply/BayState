- 2026-04-08: `validate_migration.py` requires Supabase credentials (`SUPABASE_URL` plus a supported key) for live `products_ingestion` validation; fixture-based dry-runs remain available when credentials are absent.
- 2026-04-08 Audit: `product_lines` management is not aligned with automatic cohort detection — migrations/backfill write `products_ingestion.product_line` as cohort keys like the 8-digit prefix, while the admin detail page queries rows by `productLine.name` and the API validates 6-digit `upc_prefix` values, which can force manual reconciliation.
- 2026-04-08 Audit: `apps/scraper/docs/cli.md` is not compliant with the shipped CLI surface; it documents unsupported flags/commands such as `--local`, `--sku`, `bsr cohort analyze`, and `bsr benchmark run`, while the implementation exposes `bsr batch test`, `bsr cohort visualize --input-file`, and `bsr benchmark extraction --products`.
- 2026-04-08 Fidelity check: `TwoPhaseConsolidationService` and sibling-aware prompt/context types exist, but live admin consolidation routes (`/api/admin/consolidation/submit` and `/api/admin/consolidation/scraped`) still call `submitBatch(...)` directly with plain `{ sku, sources }` payloads, so the planned second-pass consistency workflow is not wired into production backend execution.
- 2026-04-09 QA: the shipped `bsr` executable is not installed/resolvable in `apps/scraper` (`bsr --help`, `bsr batch test --help`, `bsr cohort visualize --help`, and `bsr benchmark --help` all fail with `command not found`), although `python3 -m cli.main ...` does expose the CLI surface.
- 2026-04-09 QA: authenticated browser fetches to `/api/admin/cohorts` and `/api/admin/product-lines` return HTTP 500 with schema-cache errors (`public.cohort_batches` and `public.product_lines` missing), which also leaves `/admin/cohorts/dashboard` stuck on `Error Loading Cohorts` and prevents status badge validation.
- 2026-04-09 QA: migration verification could not be completed in the current local setup — `apps/web/supabase/config.toml` is absent, `supabase status` cannot find a local `supabase_db_web` stack, `supabase db reset --local` reports `supabase start is not running`, and `supabase migration list` requires `SUPABASE_DB_PASSWORD`/linked DB access.
