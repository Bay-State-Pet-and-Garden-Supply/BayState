## Review

- Correct: production/coordinator isolation is explicit in the sandbox docs. `sandbox/product-page-extraction/README.md:7-17` says the sandbox must not integrate with `apps/web`, the scraper daemon/runner, Supabase, coordinator callbacks, production YAML publication, real `SCRAPER_API_URL` / `SCRAPER_API_KEY`, or root Turbo/package wiring.
- Correct: direct inspection did not find production DB/coordinator code paths. The only production-sensitive env names are empty placeholders in `.env.example` (`SCRAPER_API_URL`, `SCRAPER_API_KEY`, `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY` at `sandbox/product-page-extraction/.env.example:34-38`) and a validator warning list in `scripts/validate_env.py:17,59-63`; grep for Supabase/coordinator/API-key terms found no runtime use outside docs, `.env.example`, and that validator.
- Correct: the runtime dependencies stay sandbox/local-web focused: `requirements.txt:1-11` includes Crawl4AI, Playwright, BeautifulSoup, Pydantic, dotenv, Rich, httpx, PyYAML, and jsonschema only; there is no Supabase, Postgres, or BayState API client dependency.
- Correct: network access is limited to user/configured web crawling plus optional LM Studio. Crawl extraction writes local packets under `--output-dir` and calls Crawl4AI on the supplied URL (`scripts/extract_product_page.py:193-204,219-220`); sitemap discovery fetches configured official-domain robots/sitemaps (`scripts/discover_from_sitemap.py:17-21,50-67,111-117`); LM Studio defaults to `http://localhost:1234/v1` and is skipped unless enabled (`.env.example:16-25`, `scripts/validate_env.py:72-78`, `scripts/lmstudio_extract.py:16-31,56-64`).
- Correct: generated evidence directories are gitignored for the documented/default paths. `sandbox/product-page-extraction/.gitignore:1-17` ignores `.env`, `.venv/`, `outputs/*`, `agent-browser-runs/*`, logs, image/html artifacts, browser profiles, pycache, and pytest cache while keeping `.gitkeep` files. I verified actual generated files under `outputs/20260524T215137Z-example-smoke/` and `agent-browser-runs/20260524T215142Z-example-smoke/` are ignored with `git check-ignore -v`.
- Blocker: none for production DB/coordinator isolation.
- Fix worth doing now: make `scripts/agent_browser_capture.sh` default output root cwd-independent. The optional third arg defaults to `sandbox/product-page-extraction/agent-browser-runs` (`scripts/agent_browser_capture.sh:4-10`). If run from the sandbox directory without that arg, it creates `sandbox/product-page-extraction/sandbox/product-page-extraction/agent-browser-runs/...`, which I verified is not ignored by `git check-ignore` (exit 1). Use a script-relative default such as the script parent’s `../agent-browser-runs`, or default to `agent-browser-runs` consistently with the README command at `README.md:120-123`.
- Note: `/Users/nickborrello/Desktop/Projects/BayState/plan.md` was absent; `/Users/nickborrello/Desktop/Projects/BayState/progress.md` only contained prior LM Studio research notes, so this review is based on direct sandbox file inspection.
