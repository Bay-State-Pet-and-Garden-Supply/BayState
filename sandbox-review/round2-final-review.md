## Review

- Correct: Final validation commands reported by the fix worker also pass in a fresh read-only run: `python3 -m compileall scripts`, `python3 scripts/validate_env.py --strict`, sample and round2 fixture dry-runs, and `run_packet.py --dry-run` all completed. Production isolation still looks intact: `.env`, generated `outputs/`, `agent-browser-runs/`, and `__pycache__/` are ignored, and no production imports were found in reviewed sandbox scripts.

- Correct: The latest Fromm artifact now makes the image gap explicit: Crawl4AI rendered fallback found 41 images while agent-browser found 109, with 36.4% overlap and `crawl4ai_rendered_close_enough: false` (`outputs/20260525T182042Z-fixture-batch/20260525T182043Z-fromm-four-star-dog/comparison.json:2-12`). The comparison also captures the card gap: agent-browser found 50 cards while the packet had 0 (`comparison.json:21-31`). This is useful evidence for oracle.

- Blocker: Crawl4AI still is not actually capturing the shared rendered JS payload, and there is also a wrapper bug. `store_js_for_dom()` wraps the shared IIFE inside `function() { <js_code> }()` without returning it, so `result` would be `undefined` even if the sentinel div were captured (`scripts/common.py:115-127`). The Crawl4AI pass then looks for `#__sandbox_rendered__`, fails, and falls back to HTML parsing (`scripts/extract_product_page.py:399-449`); the latest rendered artifact confirms `_note: "Extraction JS sentinel not found; used HTML parser"`, `method: "scroll+html_fallback"`, and `productCardCount: 0` (`rendered-evidence.json:2-3`, `rendered-evidence.json:47-49`). Fix before treating Round 2 as conclusive: either capture Crawl4AI JS return values through the supported Crawl4AI v0.8 mechanism, or at least fix the wrapper to return the shared IIFE result and document that Crawl4AI cannot surface it after a verified attempt.

- Blocker: Page classification/card evidence is still not valid enough for the intended benchmark. The Fromm fixture expects `collection`, but the latest packet summary scored page type as `unknown` and failed the page type field (`summary.json:78-81`, `summary.json:127-130`). The classifier can classify collection when cards exist (`scripts/page_classifier.py:89-100`), but Crawl4AI packets still have no cards because rendered evidence falls back to HTML parsing. Fix worth doing now: classify known collection/category URL shapes conservatively even without cards, and separately record “card isolation failed” when agent-browser shows cards that Crawl4AI cannot capture.

- Blocker: LLM failure metrics are still lost on failure paths. `lmstudio_extract.py` constructs metrics in URL/parse/schema failure branches but raises exceptions instead of returning/attaching those metrics (`scripts/lmstudio_extract.py:100-113`, `scripts/lmstudio_extract.py:128-163`). `extract_product_page.py` catches the exception and records only `llm_skipped_reason`, leaving `llm_metrics` unset (`scripts/extract_product_page.py:463-471`). The schema also permits `llm_metrics` to be null and does not require it in `extraction` (`schemas/product_packet.schema.json:40-49`). Fix: introduce a typed exception carrying metrics, or return `(None, metrics)` for failures, and require a metrics object for every LLM mode including off/skipped/timeout/schema-fail.

- Blocker: The Round 2 fixture matrix is still mislabeled and insufficient for the oracle’s broader question. It now has 16 rows, but the `canonical_jsonld_pdp` rows are Fromm collection URLs and explicitly expect `page_type: "collection"`, not PDP/Product JSON-LD (`fixtures/products.round2.jsonl:1-2`); the `canonical_html_pdp` rows are also Fromm collection URLs expecting collection (`fixtures/products.round2.jsonl:3-4`). This can test Fromm collection behavior, but it cannot evaluate canonical PDP happy paths. Fix before full oracle review: add real PDP fixtures with Product JSON-LD and real no-JSON-LD PDPs, or rename the groups so the benchmark does not claim coverage it lacks.

- Blocker: Aggregate metrics still cannot answer by fixture category. `run_fixture.py` groups aggregates by observed page type/error only (`scripts/run_fixture.py:155-158`) rather than by fixture `group`; the latest summary consequently reports only `by_page_type.unknown` for the Fromm live run (`summary.json:2-10`). Fix: include `by_fixture_group` and group-level image close-enough rates, field pass rates, LLM failures, and recommendations so the benchmark can distinguish PDP, collection, marketing-only, and trap behavior.

- Blocker: Schemas remain too loose for Round 2 consistency. In-memory validation showed a packet with missing `extraction.llm_metrics` still validates, a packet with missing `extraction.product_cards` still validates, and a comparison with missing `page_type_comparison` still validates. The schema lines explain why: `llm_metrics` is nullable/optional (`schemas/product_packet.schema.json:48-49`), `product_cards` is not in the extraction required list (`schemas/product_packet.schema.json:40-43`, `schemas/product_packet.schema.json:80-98`), and comparison top-level required fields omit `page_type_comparison` (`schemas/comparison.schema.json:5`, `schemas/comparison.schema.json:31-34`). Fix: require the Round 2 fields that downstream comparisons and summaries depend on.

- Note: Safety improved with private LLM URL validation (`scripts/common.py:166-180`) and ignored local artifacts, but output containment is still not enforced for explicit CLI paths. `require_sandbox_path()` exists (`scripts/common.py:151-163`), but writers still use `sandbox_path()` or raw shell args for output roots (`scripts/extract_product_page.py:373-375`, `scripts/run_fixture.py:81-83`, `scripts/discover_from_sitemap.py:110-112`, `scripts/agent_browser_capture.sh:8-12`). Fix worth doing now: use `require_sandbox_path()` for all artifact output roots, with an explicit escape hatch.

## Readiness

Not ready for oracle review as a final Round 2 answer. It is useful interim evidence, but the remaining blockers mean the benchmark still cannot conclusively answer whether Crawl4AI can match agent-browser with the same rendered DOM extraction, nor can it evaluate PDP vs collection behavior across the promised fixture groups. I would fix the JS capture/wrapper, LLM failure metrics, fixture group coverage, group aggregates, schema required fields, and output containment first; then run at least one Fromm dog/cat live benchmark plus a few real PDP fixtures before sending to oracle.
