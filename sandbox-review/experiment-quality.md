## Review
- Correct: The sandbox is isolated from production code in intent and current git state. `README.md` defines the scope as evaluating Crawl4AI, LM Studio/Gemma, and agent-browser locally (`sandbox/product-page-extraction/README.md:3`) and explicitly prohibits app/scraper/Supabase/coordinator/root wiring (`README.md:9-15`). `validate_env.py` checks forbidden production env vars (`scripts/validate_env.py:17`, `scripts/validate_env.py:59-62`), and `git status --short -- sandbox/product-page-extraction sandbox-research/final-sandbox-plan.md apps/web apps/scraper packages package.json turbo.json` showed only untracked sandbox/plan files.
- Correct: The basic artifact shape is useful for future review. The packet contract documents crawl/extraction/validation/artifact/error sections (`docs/evidence-packet.md:7-13`), and schemas exist for packets, LLM output, agent-browser captures, and comparisons. The smoke packet and agent-browser capture did generate parseable JSON (`outputs/20260524T215137Z-example-smoke/packet.json:18`, `agent-browser-runs/20260524T215142Z-example-smoke/dom-extract.json:5`).
- Fixed: None. This was review-only; I only wrote this requested review artifact.

- Blocker: The current evidence cannot answer which approach works best. The experiment log records setup checks plus `example.com` smoke runs, with LM Studio explicitly skipped because `C4AI_LLM_MODE=off` (`docs/experiment-log.md:20-28`, especially `docs/experiment-log.md:23`). The generated comparison is therefore a non-product page comparison: Crawl4AI packet is `conflict` (`outputs/20260524T215137Z-example-smoke/packet.json:78`) while the browser comparison says `accept` at weighted `0.75` (`outputs/20260524T215137Z-example-smoke/comparison.json:6`, `comparison.json:13`). Fix now: add a small benchmark matrix of real PDP fixtures and run the same cases through deterministic Crawl4AI, Crawl4AI+Gemma/LM Studio, and agent-browser fallback, recording model/version, URL, pass/fail, field scores, latency, and failure modes in `docs/experiment-log.md`.

- Blocker: The fixture/orchestration path does not yet run the benchmark matrix. Fixtures define expected fields, thresholds, and agent-browser options (`scripts/run_fixture.py:15`, `fixtures/products.sample.jsonl:1-2`), but `run_fixture.py` skips all non-`known_url` discovery rows (`scripts/run_fixture.py:60-61`), exposes `--agent-browser-fallback` without using it (`scripts/run_fixture.py:43`), and summarizes only packet recommendation (`scripts/run_fixture.py:79`). `run_packet.py` selects a discovered candidate (`scripts/run_packet.py:36-41`) but only calls extraction (`scripts/run_packet.py:61`), while `extract_product_page.py` hardcodes `discovery.used=false`, `candidate_count=0`, and empty candidates (`scripts/extract_product_page.py:151`). Fix now: route every fixture through `run_packet.py`, persist discovery candidates/selected rationale into the final packet, run agent-browser/compare when requested, and make the batch summary include per-field expected-vs-actual outcomes.

- Blocker: The LM Studio/Gemma path is prompt-only, not structured-output testing. The progress notes say structured output via `response_format`/`json_schema` is the intended pattern (`progress.md:7-10`, `progress.md:20`), but `lmstudio_extract.py` builds a chat-completions payload with only model/temperature/max_tokens/messages (`scripts/lmstudio_extract.py:47-55`) and then parses/free-recovers JSON from model text (`scripts/lmstudio_extract.py:68-74`). The LLM schema also allows arbitrary extra fields and has no required keys (`schemas/product_llm.schema.json:5`). Fix now: use LM Studio’s OpenAI-compatible `response_format: {type: "json_schema", strict: true}` (or the native SDK structured response), tighten the schema, validate output before merging, and record `llm_skipped_reason`/model id for every run.

- Blocker: Scoring and comparison can produce false confidence. The deterministic extractor falls back to the input brand as an extracted field (`scripts/extract_product_page.py:118-120`), then counts it as a brand match in confidence (`scripts/extract_product_page.py:139-141`) and records evidence as `jsonld/input` (`scripts/extract_product_page.py:169-171`). The browser comparison ignores UPC/brand/description/packet recommendation and can accept based on name text plus browser success (`scripts/compare_results.py:42-47`), which is exactly what happened on `example.com` (`comparison.json:6`, `comparison.json:10-13`). Fix now: keep input hints separate from page evidence, require page-sourced evidence for scored fields, and make comparison field-level across name/brand/UPC/description/images/size/category with no `accept` when the underlying packet is `conflict` or lacks PDP evidence.

- Note: `/Users/nickborrello/Desktop/Projects/BayState/plan.md` was not present when I attempted to read it, so this review used `progress.md`, the sandbox files, generated artifacts, and the available `sandbox-research/final-sandbox-plan.md` context.
