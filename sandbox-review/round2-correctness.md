## Review

- Correct: `run_packet.py --dry-run` now returns before sitemap discovery, so the dry-run path is actually offline for discovery-mode inputs (`sandbox/product-page-extraction/scripts/run_packet.py:51-55`). I also ran the documented dry-run commands for `fixtures/products.sample.jsonl`, `fixtures/products.round2.jsonl`, and `run_packet.py --dry-run`; they completed.
- Correct: LLM calls now use LM Studio/OpenAI-compatible `response_format: {type: "json_schema"}` and validate the returned JSON against `schemas/product_llm.schema.json` (`sandbox/product-page-extraction/scripts/lmstudio_extract.py:68-84`, `sandbox/product-page-extraction/scripts/lmstudio_extract.py:146-173`).
- Correct: `accept` is gated on `page_type == "pdp"`, so collection/category/unknown pages cannot be accepted solely because they have images or an LLM description (`sandbox/product-page-extraction/scripts/extract_product_page.py:268-278`, `sandbox/product-page-extraction/scripts/extract_product_page.py:346-350`).
- Fixed: None. Review-only task; I only wrote this requested review artifact.

- Blocker: Crawl4AI's rendered pass does not execute the shared rendered-evidence JS required by the planner. The plan requires Crawl4AI to run shared `RENDERED_EVIDENCE_JS` and persist its returned payload (`sandbox-round2-plan.md:13-16`), but `extract_product_page.py` imports `RENDERED_EVIDENCE_JS` and never uses it (`sandbox/product-page-extraction/scripts/extract_product_page.py:16-18`). The rendered phase only runs a scroll snippet (`sandbox/product-page-extraction/scripts/extract_product_page.py:400-418`), then parses rendered HTML with BeautifulSoup and hard-codes `productCards: []`, `productCardCount: 0`, `method: "scroll+bs4"` (`sandbox/product-page-extraction/scripts/extract_product_page.py:425-449`). This means the Crawl4AI media comparison is not using the same extraction logic as agent-browser, misses JS/data-attribute evidence, and cannot produce product-card evidence.

- Blocker: Product-card requirements are effectively unmet in the Crawl4AI packet path. The shared JS captures card fields, but omits required `nearby_text` and diagnostics such as id/class names (`sandbox/product-page-extraction/scripts/common.py:68-89`; planner expectation at `sandbox-round2-plan.md:55-63`). The packet only calls `match_product_cards` when `rendered.productCards` exists (`sandbox/product-page-extraction/scripts/extract_product_page.py:280-283`), while the Crawl4AI rendered pass always writes an empty card list (`sandbox/product-page-extraction/scripts/extract_product_page.py:442-447`). `expected_tokens` are supported by the matcher signature but never passed from fixtures (`sandbox/product-page-extraction/scripts/extract_product_page.py:132-145`, `sandbox/product-page-extraction/scripts/extract_product_page.py:280-283`). Fix now: make Crawl4AI capture the same JS payload as agent-browser, include the missing card fields, pass fixture expected tokens/species/size into matching, and record a warning when card isolation fails.

- Blocker: Page classification is not reliable for the target Fromm collection pages because it depends on product-card count that Crawl4AI never populates. The planner acceptance says Fromm dog/cat Four-Star pages should classify as collection/category, and example.com should be low-confidence unknown/brand_home (`sandbox-round2-plan.md:45-48`). With zero cards, `/products/dog/four-star/` only gets a `deep_product_url` signal (`sandbox/product-page-extraction/scripts/page_classifier.py:43-58`), falls through to `unknown` (`sandbox/product-page-extraction/scripts/page_classifier.py:81-102`), but still gets high confidence 0.8 solely because of the deep URL signal (`sandbox/product-page-extraction/scripts/page_classifier.py:104-122`). I verified this in-memory: `classify_page('https://frommfamily.com/products/dog/four-star/', ..., {'productCards': []})` returns `{'page_type': 'unknown', 'confidence': 0.8, 'signals': ['deep_product_url']}`. Fix now: do not assign high confidence to `unknown` from `deep_product_url`, and classify collection/category from path/text/card signals once rendered cards are available.

- Blocker: Selected image logic still drops rendered images whenever a default meta/JSON-LD image exists, which contradicts the Round 2 acceptance that Fromm category `fields.images` should not remain limited to one hero image when rendered evidence has many images (`sandbox-round2-plan.md:30-33`). The code chooses `selected_images = default_images if default_images else rendered_images` (`sandbox/product-page-extraction/scripts/extract_product_page.py:205-212`) and stores that in `fields.images` (`sandbox/product-page-extraction/scripts/extract_product_page.py:224-231`). I verified with an in-memory packet that one `og:image` plus two rendered images yields `fields.images == ['hero.jpg']` while media shows rendered count 2. Fix now: for non-PDP/collection pages, either merge verified rendered images into `selected_images` or separately score/report `fields.images` from rendered evidence without letting image volume affect `accept`.

- Blocker: LLM image and hallucination safety are incomplete. The planner requires LLM images only when source-supported and requires null-correctness/hallucination checks for UPC/SKU/price/ingredients/size/images (`sandbox-round2-plan.md:30-33`, `sandbox-round2-plan.md:80-83`). The code verifies LLM images only in an initial branch (`sandbox/product-page-extraction/scripts/extract_product_page.py:210-216`), but later, if no selected images exist, copies `llm_images[:20]` into `fields.images` without source verification (`sandbox/product-page-extraction/scripts/extract_product_page.py:239-246`). The final metrics object contains only model/base URL/latency/attempts/timeout/schema/error/finish reason (`sandbox/product-page-extraction/scripts/lmstudio_extract.py:163-173`); there is no `null_correctness`, `hallucination_flags`, or `validation.llm_safety`. Fix now: remove the unverified LLM-image fallback and add explicit source-evidence checks before copying LLM-only values into packet fields.

- Blocker: LLM failure metrics are lost, so timeout/schema-failure reliability cannot be measured. The planner requires `extraction.llm_metrics` even when skipped/timed out, with `schema_validation_passed=false` and `timeout_count>=1` for timeouts (`sandbox-round2-plan.md:75-78`). `lmstudio_extract.py` builds metrics on missing model, URL errors, parse errors, and schema errors, but raises exceptions without returning those metrics (`sandbox/product-page-extraction/scripts/lmstudio_extract.py:55-66`, `sandbox/product-page-extraction/scripts/lmstudio_extract.py:98-111`, `sandbox/product-page-extraction/scripts/lmstudio_extract.py:126-161`). `extract_product_page.py` catches the exception in auto mode and only sets `llm_skipped_reason`; `llm_metrics` remains `None` (`sandbox/product-page-extraction/scripts/extract_product_page.py:454-467`, `sandbox/product-page-extraction/scripts/extract_product_page.py:313-319`). Fix now: return or attach metrics on all failure paths and store a metrics object for `off`, skipped, timeout, parse-fail, and schema-fail states.

- Blocker: Field scoring ignores `expected.required_fields`, so required fields can pass as null. Fixtures declare `expected.required_fields` (for example `description` and `images` at `sandbox/product-page-extraction/fixtures/products.round2.jsonl:6`, and `description` at lines 13-16), but `score_fixture` only maps explicit expected values (`sandbox/product-page-extraction/scripts/field_scoring.py:151-160`). `_score_description` returns pass when no expected description text is provided, even if the fixture required description (`sandbox/product-page-extraction/scripts/field_scoring.py:95-102`). I verified this in-memory: a fixture with `expected.required_fields=['brand','description']` and `description=None` scores description as passed: `No expected description; null is acceptable`. Fix now: use `required_fields` to force presence checks and include `expected`/`actual` in every field score, as requested by the planner (`sandbox-round2-plan.md:65-73`).

- Blocker: Round 2 fixture matrix and benchmark aggregation do not meet oracle/planner scope. The plan requires 15-25 rows, explicit `group`, and at least 5 JSON-LD PDPs, 5 HTML PDPs, 5 SPA collections, 3-5 marketing-only, and 3+ false-positive traps (`sandbox-round2-plan.md:98-106`). The file and docs claim 15+ rows (`sandbox/product-page-extraction/fixtures/products.round2.jsonl:1`, `sandbox/product-page-extraction/fixtures/README.md:22`, `sandbox/product-page-extraction/README.md:149-153`), but `run_fixture.py --dry-run` reports 12 rows, and the JSONL has no `group` property (`sandbox/product-page-extraction/fixtures/products.round2.jsonl:6-25`). `run_fixture.py` aggregates by resulting page type only, not fixture group, and only counts accept/review/conflict/LLM timeouts (`sandbox/product-page-extraction/scripts/run_fixture.py:142-159`), missing schema failures, hallucinations, rendered-image success, and agent-browser-added-useful-images counts required by the plan (`sandbox-round2-plan.md:118-121`). Fix now before interpreting benchmark quality.

- Blocker: Schemas are too loose to enforce Round 2 packet/CLI consistency. `product_packet.schema.json` requires only broad top-level objects and allows `llm_metrics` to be null; `media` and `product_cards` are optional inside extraction (`sandbox/product-page-extraction/schemas/product_packet.schema.json:39-54`). I verified a minimal packet with no `extraction.media` validates. `agent_browser_result.schema.json` requires `rendered` but none of its Round 2 nested fields (`images`, `productCards`, `imageCount`, `productCardCount`, `extractionMethod`) are required (`sandbox/product-page-extraction/schemas/agent_browser_result.schema.json:12-23`). This lets validation pass while the fields needed for media/page/card comparisons are absent. Fix now: require and type the Round 2 nested fields, including `image_count_by_method` keys and LLM metric keys, while keeping `additionalProperties` only where flexibility is intentional.

- Note: `agent_browser_capture.sh` still embeds an inline copy of the rendered JS (`sandbox/product-page-extraction/scripts/agent_browser_capture.sh:17-79`) instead of reading/generating it from `common.py`, so future drift is likely. If keeping a shell copy, add a simple hash/snapshot test to ensure it stays equivalent to `RENDERED_EVIDENCE_JS`.
- Note: `/Users/nickborrello/Desktop/Projects/BayState/plan.md` was not present. I reviewed against `sandbox-round2-plan.md`, `round2-worker-handoff.md`, `progress.md`, docs, scripts, schemas, and fixtures. `progress.md` still contains earlier LM Studio research notes, not the Round 2 implementation plan.
