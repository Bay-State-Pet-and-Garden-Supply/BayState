## Review

Scope note: `/Users/nickborrello/Desktop/Projects/BayState/plan.md` was not present; `/Users/nickborrello/Desktop/Projects/BayState/progress.md` currently contains unrelated LM Studio research notes. I inspected `sandbox/product-page-extraction/**` directly and did not modify sandbox/source files.

- Correct: The sandbox documents the intended production boundary clearly: no `apps/web`, scraper daemon, Supabase, coordinator callback, production YAML, real scraper API credentials, or root package/Turbo wiring (`sandbox/product-page-extraction/README.md:7-17`). The subagent workflow repeats the stop rules for touching production app/package/migration code and adding production credentials (`sandbox/product-page-extraction/docs/subagent-workflow.md:19-24`).
- Correct: Source inspection found no production imports or production package usage. A source-only grep excluding generated directories found only boundary docs plus `validate_env.py` references to forbidden env names; no `apps/web`, `apps/scraper`, `packages/api`, `@baystate`, Supabase client, database URL, or Next env imports were present.
- Correct: `.env` handling is sandbox-local. `load_dotenv()` reads only `ROOT / ".env"` where `ROOT` is the sandbox directory, and sets variables without loading root app env files (`sandbox/product-page-extraction/scripts/common.py:113-122`). `.env.example` defaults LLM mode off and LM Studio to localhost (`sandbox/product-page-extraction/.env.example:16-25`), and keeps scraper/Supabase values empty (`sandbox/product-page-extraction/.env.example:34-38`).
- Correct: The local `.env` is ignored by the nested `.gitignore` (`sandbox/product-page-extraction/.gitignore:1`) and currently leaves scraper/Supabase values empty (`sandbox/product-page-extraction/.env:33-37`). The same `.gitignore` ignores generated outputs, agent-browser runs, logs/images/html, `__pycache__/`, `*.pyc`, and `.pytest_cache/` (`sandbox/product-page-extraction/.gitignore:3-17`). `git check-ignore -v` confirmed `.env`, `outputs/...`, and `scripts/__pycache__/...pyc` are ignored by those rules.
- Correct: Defaults route generated artifacts into ignored sandbox directories: `SANDBOX_OUTPUT_DIR=./outputs` in `.env.example` (`sandbox/product-page-extraction/.env.example:1-7`), extraction writes packet/page/jsonld/rendered evidence under `out_dir` (`sandbox/product-page-extraction/scripts/extract_product_page.py:375-377`, `392-394`, `449-452`, `473-481`), fixture batches default under `outputs` (`sandbox/product-page-extraction/scripts/run_fixture.py:69`, `81-83`), and agent-browser defaults under `sandbox/product-page-extraction/agent-browser-runs` (`sandbox/product-page-extraction/scripts/agent_browser_capture.sh:6-12`).

- Blocker: None found for accidental production imports, production env consumption, or committed generated artifacts.

- Note: Fix worth doing now — output path containment is not enforced. `sandbox_path()` returns absolute paths unchanged (`sandbox/product-page-extraction/scripts/common.py:125-127`), and the writers accept `--output-dir` or an agent-browser output root before creating directories (`sandbox/product-page-extraction/scripts/extract_product_page.py:375-377`; `sandbox/product-page-extraction/scripts/discover_from_sitemap.py:106-112`; `sandbox/product-page-extraction/scripts/run_fixture.py:69`, `81-83`; `sandbox/product-page-extraction/scripts/agent_browser_capture.sh:8-12`). Defaults are safe, but a bad CLI arg can place generated packets/screenshots outside ignored `outputs/` or `agent-browser-runs/`. Recommend rejecting artifact roots outside the sandbox, or requiring an explicit escape hatch such as `--allow-outside-sandbox`.
- Note: Fix worth doing now — LM Studio is not constrained to local/private endpoints in code. `lmstudio_settings()` trusts `LMSTUDIO_BASE_URL` / `LM_STUDIO_BASE_URL` from env and sends evidence to `${base_url}/chat/completions` (`sandbox/product-page-extraction/scripts/lmstudio_extract.py:21-25`, `89-96`). The checked local `.env` uses a private LAN endpoint and `C4AI_LLM_MODE=auto` (`sandbox/product-page-extraction/.env:16-24`), which is not production, but if local-only is a hard safety requirement, add validation to permit only localhost/RFC1918 hosts unless explicitly allowed.
- Note: Generated packet/summary artifacts embed absolute local filesystem paths when `out_dir` is absolute (`sandbox/product-page-extraction/scripts/extract_product_page.py:307-308`, `333`, `352-357`; `sandbox/product-page-extraction/scripts/run_fixture.py:83`, `130-134`; `sandbox/product-page-extraction/scripts/agent_browser_capture.sh:126-134`). They are ignored, so this is not a commit blocker, but relative artifact paths would reduce local path leakage when evidence packets are shared for review.
- Note: `.gitignore` covers current generated pycache/output artifacts, but if the sandbox will run `ruff`/`mypy`, add `.ruff_cache/` and `.mypy_cache/` alongside `.pytest_cache/` (`sandbox/product-page-extraction/.gitignore:15-17`).
