## Review

- Correct: The sandbox is isolated under `sandbox/product-page-extraction`; generated evidence paths are gitignored (`sandbox/product-page-extraction/.gitignore:2-5`), and the README explicitly prohibits production integration (`sandbox/product-page-extraction/README.md:5-17`).
- Correct: Basic reproducibility checks passed locally with `python3`: AST syntax parse for every `scripts/*.py`, `scripts/validate_env.py --strict`, `scripts/run_fixture.py --fixture fixtures/products.sample.jsonl --dry-run` (2 rows), and `agent_browser_capture.sh https://example.com ... /tmp/...` followed by `validate_packet.py --kind agent-browser`.
- Fixed: None; review-only task, no sandbox source files modified.

- Blocker: `--screenshot` produces packets that point at a missing file. `extract_product_page.py` enables Crawl4AI screenshots (`sandbox/product-page-extraction/scripts/extract_product_page.py:197`) and writes `crawl.screenshot_path` when `args.screenshot` is true (`sandbox/product-page-extraction/scripts/extract_product_page.py:158`), but never persists `result.screenshot`; `artifacts.screenshot` is hard-coded to `None` (`sandbox/product-page-extraction/scripts/extract_product_page.py:184`). I verified a live `https://example.com` run with `--screenshot` generated a schema-valid packet whose `screenshot_path` did not exist. Fix now: save the Crawl4AI screenshot bytes/base64 to `out_dir/screenshot.png` and populate both packet paths, or remove/disable the screenshot CLI/fixture option until implemented.

- Blocker: `run_packet.py --dry-run` is not dry in discovery mode. The code performs sitemap discovery before checking `args.dry_run` (`sandbox/product-page-extraction/scripts/run_packet.py:36-43`), and `discover()` fetches robots/sitemaps over the network (`sandbox/product-page-extraction/scripts/discover_from_sitemap.py:55-76`). Fix now: move the dry-run branch before live discovery, or make dry-run return only parsed config/input plus the supplied URL without network calls.

- Blocker: Discovery evidence is lost from the final packet. `run_packet.py` computes `candidates` and `selected_url` (`sandbox/product-page-extraction/scripts/run_packet.py:36-41`) but passes only a known-url-style namespace into `extract_run` (`sandbox/product-page-extraction/scripts/run_packet.py:48-61`). `build_packet()` then hard-codes `discovery.used: false`, `candidate_count: 0`, and empty candidates (`sandbox/product-page-extraction/scripts/extract_product_page.py:151`), which contradicts the packet contract that discovery should include candidate URLs and selection rationale (`sandbox/product-page-extraction/docs/evidence-packet.md:7-8`). Fix now: pass discovery metadata into extraction and populate `discovery` accurately.

- Blocker: LLM “structured” output is prompt-only and not schema-enforced. `lmstudio_extract.py` sends a plain chat payload with messages only (`sandbox/product-page-extraction/scripts/lmstudio_extract.py:47-55`), embeds the schema in user text (`sandbox/product-page-extraction/scripts/lmstudio_extract.py:83-85`), parses returned JSON (`sandbox/product-page-extraction/scripts/lmstudio_extract.py:68-75`), and `extract_product_page.py` merges returned keys without validating them (`sandbox/product-page-extraction/scripts/extract_product_page.py:131-136`). The LLM schema itself has no `required` keys and allows arbitrary extra properties (`sandbox/product-page-extraction/schemas/product_llm.schema.json:4-6`); I verified `validate_packet.py --kind llm` accepts `{}`. Fix now: use LM Studio/OpenAI `response_format` JSON schema where available, validate the parsed result with `jsonschema` before merge, and tighten the schema enough that malformed or empty LLM output cannot silently pass.

- Blocker: Several advertised CLI/config knobs are currently no-ops or not loaded from `.env`, which makes runs hard to reproduce from the provided samples. `.env.example` advertises `SANDBOX_OUTPUT_DIR`, `SANDBOX_PAGE_TIMEOUT_MS`, and `C4AI_LLM_MODE` (`sandbox/product-page-extraction/.env.example:2-17`), but `OUTPUTS` is fixed to `ROOT / "outputs"` (`sandbox/product-page-extraction/scripts/common.py:16-17`). `extract_product_page.py` reads `C4AI_LLM_MODE` and `SANDBOX_PAGE_TIMEOUT_MS` while defining parser defaults (`sandbox/product-page-extraction/scripts/extract_product_page.py:234-237`), before `.env` is loaded inside `run()` (`sandbox/product-page-extraction/scripts/extract_product_page.py:189-190`), so values from a copied `.env` do not affect those defaults. `run_packet.py` also exposes `--extraction-config` but never reads it (`sandbox/product-page-extraction/scripts/run_packet.py:19-31`), and `run_fixture.py` exposes `--agent-browser-fallback` but never uses it (`sandbox/product-page-extraction/scripts/run_fixture.py:42-44`, `sandbox/product-page-extraction/scripts/run_fixture.py:56-80`). Fix now: centralize settings after `load_dotenv()` and either wire these flags/env vars through the live flow or remove them from samples/CLIs until supported.

- Note: `/Users/nickborrello/Desktop/Projects/BayState/plan.md` was not present when requested; `progress.md` only contained earlier LM Studio research notes, not a product-page-extraction implementation plan.
