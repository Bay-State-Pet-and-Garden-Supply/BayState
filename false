# Pipeline Simplification: Remove needs_fallback_review

## Changes Made

1. **`apps/web/lib/pipeline/types.ts`** — Removed `needs_fallback_review` from PIPELINE_TABS (7 tabs now: imported, scraping, scraped, consolidating, finalizing, exporting, failed). Removed its STAGE_CONFIG entry. Kept in PERSISTED_PIPELINE_STATUSES for DB compatibility.

2. **`apps/web/lib/pipeline/core.ts`** — Updated STATUS_TRANSITIONS: `scraping` → `['scraped', 'failed', 'imported']`, `scraped` → `['consolidating', 'finalizing', 'imported', 'failed']`. Removed `needs_fallback_review` entry entirely. Added guard for undefined transition entries.

3. **`apps/web/lib/pipeline/core.test.ts`** — Updated transition matrix expectations.

4. **`apps/web/app/api/scraper/v1/chunk-callback/route.ts`** — All failed static products route to `imported` instead of `needs_fallback_review`. Quality evaluation still runs but failures → `imported`.

5. **`apps/web/components/admin/pipeline/PipelineClient.tsx`** — Removed FallbackReviewView import, rendering block, WORKSPACE_TABS entry, and Globe button/fallbackReviewHref.

6. **`apps/web/components/admin/pipeline/StageTabs.test.tsx`** — Updated to 7 tabs, removed fallback review count.

7. **`apps/web/__tests__/accessibility/pipeline-a11y.test.tsx`** — Updated tab count from 8 to 7, removed Fallback Review assertion.

8. **`apps/web/lib/pipeline/derivation.ts`** — Removed needs_fallback_review from WORKFLOW_PIPELINE_TABS and deriveTabFromProduct.

9. **`apps/web/lib/pipeline/derivation.test.ts`** — Updated expectations.

10. **`apps/web/__tests__/lib/pipeline-status-validation.test.ts`** — Updated transition matrix to match core.ts.

11. **`apps/web/lib/pipeline/queries.ts`** — Removed needs_fallback_review from workflow tab counts query.

12. **`apps/web/lib/pipeline/queries.test.ts`** — Updated to match.

## Validation

- **54 suites pass, 10 pre-existing failures** (all confirmed pre-existing on clean master: image-retry-flow, ScrapedResultsView, StatusBadge, AlertBanner, finalization-draft, images.test, sku-route, publish-route, credentials.route, active-consolidations)
- **422 tests pass, 28 pre-existing failures**
- **0 new failures introduced**

## What stays

- Static-only scraping (no enrichment method choices)
- Results tab (renamed from Scraped)
- Provenance on sources
- searching/url_review/extracting as non-tab fallback operational statuses
- Fallback API and orchestration available for admin-initiated use from imported
