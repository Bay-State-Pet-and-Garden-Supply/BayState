## Review
- Correct: The core Phase 1 data-flow fix is small and targeted. `apps/scraper/scrapers/ai_search/enrichment_models.py:334-343` now prefers `data_url` for successful capture dicts and skips errored captures, avoiding private vendor URL leakage. The web callback test also proves nested `source_results[].product.media[].url` data URLs are converted to Supabase Storage URLs.
- Correct: The R2 work stayed lightweight: no provider files, S3/R2 dependencies, lockfile changes, or scraper-side storage credentials were introduced. Current changes only add durable URL recognition in `apps/web/lib/product-image-storage.ts:15-31,148-160` and `apps/scraper/scrapers/approved_sources/image_capture.py:5-31`.
- Correct: Focused checks passed: `python3 -m pytest tests/unit/test_image_capture.py tests/unit/test_approved_sources_result_builder.py -q` (29 passed) and `cd apps/web && bun run test -- __tests__/app/api/scraper/v1/enrichment-callback-route.test.ts __tests__/scripts/backfill-login-protected-images.test.ts lib/__tests__/product-image-storage.test.ts` (17 passed). `bun run typecheck` and `git diff --check` were clean.

- Blocker (High): The backfill CLI still defaults to execute mode even though the README now says dry-run is the default and also warns `image_retry_queue` has no consumer. `apps/web/scripts/backfill-login-protected-images-logic.ts:524-566` sets `mode: 'execute'` and describes `--execute` as the default, while `apps/web/scripts/README.md:43-48,65-72` says queue entries do not repair products and dry-run is default. A bare documented run would insert dead-end retry rows.

- Note (Medium): Catalog-derived auth source detection does not include aliases, despite the approved plan calling out aliases. `resolveLoginProtectedSlugsFromCatalog()` returns only `entry.sourceSlug` (`apps/web/scripts/backfill-login-protected-images-logic.ts:109-112`), and matching is exact (`:191-197`). Catalog aliases such as `orgill_crawl4ai`, `phillips_crawl4ai`, and `petfoodex` exist in `apps/web/lib/approved-sources/distributor-catalog.ts:122,145,174-178`, so existing rows keyed by aliases can be missed unless they also carry `requires_login`.

- Note (Medium): Future R2/custom URL recognition is incomplete and duplicated. The web storage path and scraper now recognize `PRODUCT_IMAGE_PUBLIC_BASE_URL`, but the backfill detector still treats only data URLs and Supabase Storage URLs as durable (`apps/web/scripts/backfill-login-protected-images-logic.ts:72-83`). Future R2 URLs in `products_ingestion.sources` would be reclassified as non-durable and queued for retry.

- Note (Low): When errored capture entries are skipped, media roles still use the original array index (`apps/scraper/scrapers/ai_search/enrichment_models.py:328,342,350`). If the first capture fails and the second succeeds, the only emitted image is marked `additional` instead of `primary`.