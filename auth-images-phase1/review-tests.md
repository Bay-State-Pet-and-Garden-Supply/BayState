## Review

- Correct: Scraper-side regression tests directly cover the root mapping contract: successful capture dicts keep `data_url` out to `product.media`/`evidence.selected_images` and exclude `shop.phillipspet.com` (`apps/scraper/tests/unit/test_approved_sources_result_builder.py:41-69`); errored capture dicts are skipped without leaking `original_url` (`apps/scraper/tests/unit/test_approved_sources_result_builder.py:71-106`). Backfill tests now cover catalog aliases and custom public-base durability (`apps/web/__tests__/scripts/backfill-login-protected-images.test.ts:13-25`, `117-145`).
- Correct: Validation I ran on the current diff: `cd apps/scraper && python3 -m pytest tests/unit/test_image_capture.py tests/unit/test_approved_sources_result_builder.py tests/unit/test_approved_sources_adapter_fixtures.py -q` => 49 passed, 6 skipped; `cd apps/web && bun run test -- lib/__tests__/product-image-storage.test.ts __tests__/app/api/scraper/v1/enrichment-callback-route.test.ts __tests__/scripts/backfill-login-protected-images.test.ts` => 20 passed; `cd apps/web && bun run typecheck` => passed. The plan's literal `python -m pytest ...` failed locally because no `python` binary is on PATH; `python3` works.
- Fixed: None applied; review-only.
- Blocker: None.
- Note (High): The callback durability test still does not prove the full admin/storefront durability path. Its fixture puts the inline URL only in `source_results[0].product.media` while top-level `product` contains only `name` (`apps/web/__tests__/app/api/scraper/v1/enrichment-callback-route.test.ts:475-496`), then asserts only `updatedSources.phillips.media[0].url` and merely that `updatedSources.enriched` exists (`apps/web/__tests__/app/api/scraper/v1/enrichment-callback-route.test.ts:512-523`). It does not assert `sources.enriched.images`, `image_urls`, `extracted.media`, `evidence.selected_images`, or embedded `enriched.source_results` contain durable URLs, and it does not assert the mocked upload was called despite tracking uploads (`apps/web/__tests__/app/api/scraper/v1/enrichment-callback-route.test.ts:80-94`).
- Note (Medium): The result-builder tests miss a primary-image role edge. `build_nested_product_facts()` assigns role from the original image index (`apps/scraper/scrapers/ai_search/enrichment_models.py:328`, `350`), so if the first capture is an error and the second succeeds, the only surviving image is marked `additional`; the current error test has that ordering but only checks URL/length, not role (`apps/scraper/tests/unit/test_approved_sources_result_builder.py:80-106`).
- Note (Low): The web product-storage custom-base test is vacuous. It passes an ordinary HTTPS URL through `replaceInlineImageDataUrls()` (`apps/web/lib/__tests__/product-image-storage.test.ts:70-87`), but that visitor returns every non-inline string unchanged before the new durable-URL branch matters (`apps/web/lib/product-image-storage.ts:459-463`). This would still pass if `PRODUCT_IMAGE_PUBLIC_BASE_URL` handling in `isProductImageStorageUrl()` were removed (`apps/web/lib/product-image-storage.ts:145-158`).
