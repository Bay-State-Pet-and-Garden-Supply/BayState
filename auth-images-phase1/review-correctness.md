## Review
- Correct: Successful capture dicts with `status == "success"` and a non-empty `data_url` now keep the inline image through the scraper contract (`apps/scraper/scrapers/ai_search/enrichment_models.py:332-340`), and `EvidenceData.selected_images` is derived from the resulting media URLs (`apps/scraper/scrapers/ai_search/enrichment_models.py:353-357`). New scraper tests cover the success and error cases (`apps/scraper/tests/unit/test_approved_sources_result_builder.py:41-107`).
- Correct: The web callback still runs `replaceInlineImageDataUrls()` over `updatedSources` before persisting (`apps/web/app/api/scraper/v1/enrichment-callback/route.ts:322-329`), and the storage helper uploads inline data URLs to the `product-images` bucket and replaces them with public URLs (`apps/web/lib/product-image-storage.ts:240-274`, `apps/web/lib/product-image-storage.ts:476-481`). The callback regression test verifies `sources.phillips.media[0].url` becomes a Supabase public storage URL, not `data:image` or `shop.phillipspet.com` (`apps/web/__tests__/app/api/scraper/v1/enrichment-callback-route.test.ts:435-520`).
- Correct: Backfill detection is now repo-appropriate: catalog-derived auth slugs include aliases such as `petfoodex` and `*_crawl4ai` (`apps/web/scripts/backfill-login-protected-images-logic.ts:121-130`), source matching uses that set (`apps/web/scripts/backfill-login-protected-images-logic.ts:209-222`), custom public bases are treated as durable (`apps/web/scripts/backfill-login-protected-images-logic.ts:87-108`), and dry-run is the default (`apps/web/scripts/backfill-login-protected-images-logic.ts:542-556`). Tests cover aliases and custom durable-base detection (`apps/web/__tests__/scripts/backfill-login-protected-images.test.ts:13-25`, `apps/web/__tests__/scripts/backfill-login-protected-images.test.ts:117-147`).
- Fixed: None applied; review only.
- Blocker (High): Protected vendor URLs can still leak from capture-shaped dicts that have capture metadata but lack a usable `data_url`. `build_nested_product_facts()` falls through from non-error statuses to `original_url` (`apps/scraper/scrapers/ai_search/enrichment_models.py:338-348`), then mirrors that URL into evidence (`apps/scraper/scrapers/ai_search/enrichment_models.py:353-357`). I reproduced this with `product_fields={"image_urls":[{"status":"success","original_url":"https://shop.phillipspet.com/private.jpg"}]}` and the resulting media/evidence contained the Phillips URL. This violates the plan requirement to use `original_url` only for plain metadata dicts with no capture status/data fields.
- Note (Medium): There is still a broader leak path if authenticated capture fails before returning structured per-image error objects: the base and Phillips adapters log the capture failure but leave the original protected string URLs in `det_result.product["image_urls"]` (`apps/scraper/scrapers/approved_sources/adapters/base.py:737-758`, `apps/scraper/scrapers/approved_sources/adapters/phillips.py:466-473`), and string image entries are emitted unchanged by `build_nested_product_facts()` (`apps/scraper/scrapers/ai_search/enrichment_models.py:329-351`).
- Note (Low): When an errored first capture is skipped, the next successful image keeps role `additional` because role uses the original array index (`apps/scraper/scrapers/ai_search/enrichment_models.py:328-351`). If downstream treats `primary` specially, assign role from `len(media) == 0` after skips.
- Note (Medium): The README root command appears wrong. `apps/web/scripts/README.md:59-63` says to run `bun run web ts-node -- apps/web/scripts/backfill-login-protected-images-logic.ts ...` from repo root; executing that command failed by resolving under `apps/web/apps/web/...`. `bun run web scripts/backfill-login-protected-images-logic.ts --help` works from the repo root.
- Tests run: `cd apps/scraper && python3 -m pytest tests/unit/test_image_capture.py tests/unit/test_approved_sources_result_builder.py -q` (29 passed); `cd apps/web && bun run test -- __tests__/app/api/scraper/v1/enrichment-callback-route.test.ts __tests__/scripts/backfill-login-protected-images.test.ts lib/__tests__/product-image-storage.test.ts` (20 passed); `cd apps/web && bun run typecheck` (passed).
