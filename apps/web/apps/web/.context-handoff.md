**Done.** Wrote `/Users/nickborrello/Desktop/Projects/BayState/apps/web/.context-handoff.md`

**Actions:**
- Read research brief (`.research-brief.md`) — full gap analysis already complete
- Traced payment flow end-to-end across 12 files (checkout → intent → Stripe → webhook → orders)
- Mapped all relevant files, configs, env, and constraints
- Produced actionable handoff with priority ordering, file-level edit targets, test areas, and validation checklist

**Key findings:**
- 4 P0 blockers preventing functional local dev (production env values, missing seed.sql, auth redirect mismatch)
- 4 P1 payment correctness/security gaps (client-authoritative amount, no webhook idempotency, no duplicate PI guard, no idempotency key)
- 3 P2 documentation/DX gaps (no Stripe CLI docs, no local env template, suspect API version string)