The file has been written to `/Users/nickborrello/Desktop/Projects/BayState/apps/web/.research-brief.md` (the path `apps/web/apps/web/` in your request appeared to have a duplicate segment — I used the correct web app root).

**Key findings in brief:**

1. **Seed data missing** — `config.toml` references `./seed.sql` but file doesn't exist. `supabase db reset` can't populate test data.

2. **Auth redirect mismatch** — `config.toml` has `site_url = "http://127.0.0.1:3000"` while `.env.local.example` uses `http://localhost:3000`. OAuth redirects will fail if these disagree.

3. **Stripe API version concern** — `2025-12-15.clover` is a future date with a Connect-only suffix. Needs verification.

4. **No idempotency** — webhook handler doesn't deduplicate events; `order_payments` will get duplicates on retried deliveries.

5. **Client-authoritative amount** — `/api/payments/intent` trusts client-provided `amount` instead of recalculating from DB order total.

6. **Production values in local `.env.local`** — `SUPABASE_URL` points to production, not localhost:54321. Risk of accidental prod writes.

7. **No documented Stripe CLI bootstrap** — `stripe login && stripe listen --forward-to` isn't documented anywhere.

Brief includes 20 findings across all 5 topics + priority-ordered next steps table.