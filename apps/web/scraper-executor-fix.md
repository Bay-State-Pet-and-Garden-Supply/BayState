# Executor AGENTS.md Fix

**File:** `apps/scraper/scrapers/executor/AGENTS.md`
**Time:** 2026-05-10

## Changes

Two instances of `~589 lines` → `~723 lines`:

1. **Structure tree** (line 3): `workflow_executor.py   # Main orchestrator (~589 lines)` → `~723 lines`
2. **Architecture list** (line 13): `WorkflowExecutor (589 lines)` → `(723 lines)`

Both applied via single edit call, 2 blocks.
