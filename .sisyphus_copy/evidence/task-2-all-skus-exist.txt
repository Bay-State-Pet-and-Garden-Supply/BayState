Command:
cd BayStateApp && CI=true npm test -- --testPathPatterns="products-ingestion-callback" --testNamePattern="updates all rows when every target SKU exists"

Result:
PASS __tests__/lib/scraper-callback/products-ingestion-callback.test.ts
  persistProductsIngestionSourcesStrict
    ✓ updates all rows when every target SKU exists (2 ms)

Assertion:
- Existing SKU set succeeds.
- Writes are invoked for each SKU.
