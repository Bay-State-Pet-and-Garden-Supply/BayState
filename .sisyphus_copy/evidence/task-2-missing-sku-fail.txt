Command:
cd BayStateApp && CI=true npm test -- --testPathPatterns="products-ingestion-callback" --testNamePattern="strict-fails with zero writes when any target SKU is missing"

Result:
PASS __tests__/lib/scraper-callback/products-ingestion-callback.test.ts
  persistProductsIngestionSourcesStrict
    ✓ strict-fails with zero writes when any target SKU is missing (1 ms)

Assertion:
- Mixed existing/missing SKU set fails with MissingProductsIngestionSkusError.
- Zero writes occur when any target SKU is missing.
