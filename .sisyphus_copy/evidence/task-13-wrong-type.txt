Task 13: Wrong Type/Shape - 400/422 Response
==============================================
Date: 2026-02-18

Admin Payloads - Wrong Types (12 tests):
- job_id as number → 400
- job_id as array → 400
- job_id as object → 400
- status as number → 400
- status as object → 400
- results as array → 400
- results as string → 400
- results.data as array → 400
- results.data as string → 400
- skus_processed as string → 400
- scrapers_run as object → 400
- scrapers_run non-string elements → 400

Chunk Payloads - Wrong Types (7 tests):
- chunk_id as number → 400
- chunk_id as array → 400
- chunk_id as object → 400
- job_id as number → 400
- results as array → 400
- results.data as array → 400
- results.data as string → 400

All wrong types return validation failures.
No persistence side effects.

Tests: 19 passed

