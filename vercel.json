{
  "bunVersion": "1.x",
  "framework": "nextjs",
  "installCommand": "bun install --frozen-lockfile",
  "buildCommand": "bun run build",
  "ignoreCommand": "bunx turbo-ignore",
  "crons": [
    {
      "path": "/api/cron/image-retry",
      "schedule": "*/5 * * * *"
    }
  ]
}