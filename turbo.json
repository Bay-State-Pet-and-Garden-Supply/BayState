{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "db:start": {
      "cache": false
    },
    "local:sync-env": {
      "dependsOn": ["db:start"],
      "cache": false
    },
    "dev": {
      "dependsOn": ["local:sync-env"],
      "cache": false,
      "persistent": true
    },
    "stripe:listen": {
      "cache": false,
      "persistent": true
    },
    "build": {
      "dependsOn": ["^build"],
      "env": ["VERCEL", "NEXT_PUBLIC_SUPABASE_URL", "NEXT_PUBLIC_SUPABASE_ANON_KEY"],
      "outputs": [".next/**", "!.next/cache/**", ".vercel/output/**", "dist/**"]
    },
    "test": {
      "dependsOn": ["build"],
      "outputs": []
    },
    "lint": {
      "outputs": []
    },
    "typecheck": {
      "dependsOn": ["^typecheck"],
      "outputs": []
    }
  }
}
